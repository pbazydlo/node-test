#!/usr/bin/env node
var debug = require('debug')('VisualStudioNodeExpress');
var app = require('../app');

app.set('port', process.env.PORT0 || 3000);

var server = app.listen(app.get('port'), function() {
    debug('Express server listening on port ' + server.address().port);
});
